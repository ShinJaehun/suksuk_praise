<section class="mx-auto w-full max-w-2xl py-6">
  <h1 class="text-2xl font-extrabold text-slate-900"><%= t("school_closures.new.title", school: @school.name) %></h1>
  <div class="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", school: @school, school_closure: @school_closure %>
  </div>
</section>
