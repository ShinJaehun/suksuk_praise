<% if turbo_frame_request? %>
  <%= render "modal", school: @school, title: "새 학교 등록", submit_label: "학교 등록" %>
<% else %>
  <section class="mx-auto w-full max-w-xl space-y-4 pt-4">
    <div class="rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
      <div class="space-y-4">
        <%= render "content", school: @school, title: "새 학교 등록", submit_label: "학교 등록" %>
      </div>
      <%= link_to "교실로 돌아가기", classrooms_path, class: "mt-4 inline-block text-sm text-blue-600 hover:underline" %>
    </div>
  </section>
<% end %>
