<%= turbo_frame_tag dom_id(@source, :activity_notes), class: "contents" do %>
  <%= render "student_activity_notes/form",
        note: @note,
        source: @source,
        return_to: @return_to %>
<% end %>
