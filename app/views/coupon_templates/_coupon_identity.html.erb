<%# locals: tpl: %>
<div class="flex min-w-0 items-center gap-3">
  <%= coupon_thumbnail(
        tpl,
        css_class: "h-12 w-12 shrink-0 rounded-xl border border-slate-200 bg-white object-cover"
      ) %>
  <div class="min-w-0 break-words text-left text-sm font-semibold leading-5 text-slate-900 lg:truncate">
    <%= tpl.title %>
  </div>
</div>
