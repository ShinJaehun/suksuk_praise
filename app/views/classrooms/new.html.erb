<section class="mx-auto w-full max-w-5xl space-y-6 pt-4">
  <div class="space-y-1">
    <h1 class="text-2xl font-extrabold tracking-tight text-slate-900"><%= t("classrooms.new.title") %></h1>
  </div>

  <%= render "form", classroom: @classroom, submit_label: t("ui.buttons.new_classroom") %>
</section>
