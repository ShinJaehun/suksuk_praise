<section class="mx-auto w-full max-w-5xl pt-4">
  <div class="mb-6 flex flex-col items-start justify-between gap-3 sm:flex-row sm:items-center">
    <h1 class="text-2xl font-extrabold tracking-tight text-slate-900"><%= @classrooms_index_title %></h1>

    <% if policy(Classroom).create? %>
      <%= link_to t('ui.buttons.new_classroom'),
            new_classroom_path,
            class: "inline-flex w-full justify-center items-center rounded-lg bg-emerald-600 px-4 py-2 text-sm font-semibold text-white shadow hover:bg-emerald-700 sm:w-auto" %>
    <% end %>
  </div>

  <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
    <% if @classrooms.any? %>
      <ul class="divide-y divide-slate-100">
        <% @classrooms.each do |classroom| %>
          <li class="flex items-center justify-between gap-3 px-5 py-4">
            <span class="min-w-0 font-semibold text-slate-800">
                <%= classroom.name %>
            </span>

            <span class="flex shrink-0 items-center gap-2">
              <%= link_to "열기",
                    classroom_path(classroom),
                    class: "inline-flex items-center rounded-lg border border-slate-200 px-3 py-1.5 text-sm font-semibold text-slate-700 hover:bg-slate-50" %>
              <% if @manageable_classroom_ids.include?(classroom.id) %>
                <%= link_to "관리",
                      edit_classroom_path(classroom),
                      class: "inline-flex items-center rounded-lg bg-slate-900 px-3 py-1.5 text-sm font-semibold text-white hover:bg-slate-800" %>
              <% end %>
            </span>
          </li>
        <% end %>
      </ul>
    <% else %>
      <div class="px-5 py-10 text-center text-sm text-slate-500">
        아직 교실이 없습니다.
      </div>
    <% end %>
  </div>
</section>
